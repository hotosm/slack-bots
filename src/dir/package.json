{
  "name": "dir",
  "version": "1.0.0",
  "description": "Returns a list of existing slack channels",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hotosm/slack-bots.git"
  },
  "author": "d-rita",
  "license": "ISC",
  "dependencies": {
    "@slack/web-api": "^6.0.0",
    "node-fetch": "^2.6.1"
  }
}
